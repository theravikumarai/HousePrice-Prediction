name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test-and-build:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Validate FastAPI application
        run: |
          python -c "from app.main import app; print('FastAPI application loaded successfully')"

      - name: Build backend Docker image
        run: |
          docker build -t house-price-backend .

      - name: Build frontend Docker image
        run: |
          docker build -t house-price-frontend -f frontend/Dockerfile .
apiVersion: v1
kind: Service

metadata:
  name: house-price-frontend-lb
  namespace: house-price

spec:
  type: LoadBalancer

  selector:
    app: house-price-frontend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8501